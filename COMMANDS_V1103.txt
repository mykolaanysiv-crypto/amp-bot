AMP XP / «АМПасадори» v1.10.3 — DEPLOY / QA

1) ЛОКАЛЬНА ПЕРЕВІРКА
python -m venv .venv
source .venv/bin/activate
python -m pip install -r requirements.txt
python -m compileall -q app scripts tests
pytest -q
cat VERSION.txt
# очікується: 1.10.3

2) ЛОКАЛЬНИЙ ЗАПУСК
python run_all.py

3) HEROKU — ПЕРЕВІРИТИ CONFIG VARS
heroku config:get BOT_TOKEN -a <APP>
heroku config:get DATABASE_URL -a <APP>
heroku config:get TIMEZONE -a <APP>
heroku config:get WEB_SESSION_SECRET -a <APP>
# TIMEZONE рекомендовано: Europe/Kyiv

4) DEPLOY ЧЕРЕЗ GIT
 git status
 git add .
 git commit -m "AMP XP v1.10.3 data integrity and data quality"
 git push heroku main

5) ПІСЛЯ DEPLOY
heroku ps -a <APP>
heroku logs --tail -a <APP>
# /health має показати 1.10.3

6) SMOKE CHECK
- Dashboard → Якість даних: canonical directory створено.
- Runtime Settings → Події: check-in open before = 60, close after = 360.
- Майбутня подія >60 хв: user QR та scanner НЕ зараховують attendance/XP.
- Подія в window: check-in працює.
- Повторний confirm не дає XP вдруге.
- Override поза window у web вимагає причину і створює audit record.
- Місячний report не рахує future attendance у Visits / Avg attendance.

ВАЖЛИВО
Нових обов'язкових Config Vars немає. Додано 1 таблицю settlement_references; вона створюється idempotently під час DB init.
