AMP v1.17.0.1 — QR badge scanner hotfix verification
Date: 2026-09-19

ROOT CAUSE
- Telegram Mini App scanner POSTed to `/tg/event-сканер/{event_id}/scan`.
- FastAPI route is `/tg/event-scanner/{event_id}/scan`.
- The camera could decode a badge and vibrate, but the attendance backend endpoint was not reached, so the participant status did not change to `attended`.

FIX
- POST URL corrected to canonical ASCII `/tg/event-scanner/{event_id}/scan`.
- Scanner now closes briefly after a decoded QR, shows a visible result, then reopens automatically.
- Successful confirmation text: `✅ Код скановано успішно` + participant name + attendance result.
- Counter renamed to `Відмічено` and increments only for `attended=true`.
- Unregistered users are shown as warning and keep the existing `Зареєструвати та підтвердити` flow.
- Regression assertions added against the Cyrillic route typo.

PASS
- python -m compileall -q app scripts tests migrations
- python -m scripts.production_preflight -> Production preflight OK for AMP v1.17.0.1
- pytest -q tests/test_v1824_telegram_scanner.py -> 4 passed
- alembic heads -> 20260919_0007 (head)
- static scanner assertions: canonical POST URL, no Cyrillic route, visible success text, auto reopen, attended counter -> PASS

ENVIRONMENT LIMITATIONS
- Full pytest collection is not available in this sandbox because `aiogram` is not installed.
- Async DB behavior tests cannot run locally because `aiosqlite` is not installed.
- GitHub Production Gate / requirements-dev remains the authoritative full test environment.

DB
- No migration required.
- Alembic head remains 20260919_0007.
