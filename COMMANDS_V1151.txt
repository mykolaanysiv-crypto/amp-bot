AMP XP / «АМПасадори» v1.15.1 — deployment commands

cd ~/Downloads/amp_bot_v1103
source .venv/bin/activate

# 1. Production backup
heroku pg:backups:capture -a amp-bot-ver-1-5-0
heroku pg:backups -a amp-bot-ver-1-5-0

# 2. Extract downloaded build
rm -rf /tmp/amp_bot_v1151
mkdir -p /tmp/amp_bot_v1151
unzip -q ~/Downloads/amp_bot_v1151.zip -d /tmp/amp_bot_v1151

# 3. Overlay while preserving repo/env
rsync -av \
  --exclude='.git' \
  --exclude='.env' \
  --exclude='.venv' \
  --exclude='__pycache__' \
  --exclude='.pytest_cache' \
  /tmp/amp_bot_v1151/amp_bot_v1151/ ./

# 4. Local checks
cat VERSION.txt
python -m compileall -q app scripts tests migrations
python -m scripts.production_preflight
pytest -q \
  tests/test_v1151_management_ux.py \
  tests/test_v1150_targeting_event_analytics.py \
  tests/test_v1104_donations_registrations_ui.py \
  tests/test_v1130_architecture_completion.py \
  tests/test_v1131_profile_badges_analytics_ux.py \
  tests/test_v1140_ambassador_donation_xp.py \
  tests/test_v11401_release_schema_order.py \
  tests/test_v11402_profile_import.py

# 5. Commit + push
git status --short
git diff --stat
git add -A
git commit -m "Upgrade AMP to v1.15.1 survey and opportunity management UX"
git push origin main

# 6. After GitHub Actions / deploy
heroku releases -a amp-bot-ver-1-5-0 | head -10
heroku ps -a amp-bot-ver-1-5-0
heroku run -a amp-bot-ver-1-5-0 -- alembic current

# Expected:
# 20260918_0005 (head)

curl -sS https://amp-bot-ver-1-5-0-9632a1434a6d.herokuapp.com/health/ready | python3 -m json.tool
curl -sS -o /tmp/amp_dependencies.json -w 'HTTP %{http_code}\n' https://amp-bot-ver-1-5-0-9632a1434a6d.herokuapp.com/health/dependencies
cat /tmp/amp_dependencies.json | python3 -m json.tool
heroku logs --dyno worker -n 150 -a amp-bot-ver-1-5-0
