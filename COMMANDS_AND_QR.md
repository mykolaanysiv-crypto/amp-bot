# Команди та QR — поточний реліз v1.10.4

Актуальна інструкція запуску й деплою: **`COMMANDS_V1104.txt`**.

QR-логіка v1.10.3 збережена: відмітка на події працює тільки у налаштованому часовому вікні, а ручний виняток у вебпанелі потребує причини й фіксується в журналі дій.

У v1.10.4 додано **💙 Підтримати**, донати/Monobank, окремі **📝 Реєстрації**, фото можливостей та UI-виправлення. Перед деплоєм: PGBackup → залежності → `compileall` → `pytest -q` → push → `/health` → логи.

Для автоматичної синхронізації банки `MONOBANK_TOKEN` задається тільки як секретна змінна середовища Heroku. Не записуйте його в Git.
