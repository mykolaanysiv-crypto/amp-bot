АМП XP / «АМПасадори» v1.7.2 — фінальний звіт перевірки

СТАН: PASS (статичні та export smoke-tests)

Перевірено:
1. VERSION.txt = 1.7.2 та APP_VERSION = 1.7.2.
2. Python compileall для app/ і scripts/ — PASS.
3. AST parse: 39 Python-файлів — PASS.
4. Jinja parse: 37 HTML-шаблонів — 0 помилок.
5. CSS: tinycss2 parse admin.css — 0 синтаксичних помилок.
6. FastAPI static route scan: 135 GET/POST маршрутів — дублікатів немає.
7. Ліги: перевірені межі 0/99/100/249/250/499/500/999/1000/1499/1500 — PASS.
8. База: SQLAlchemy schema містить 40 таблиць; v1.7.2 не додає таблиць/колонок відносно v1.7.1; compatibility migration повторно виконується без помилки у sync smoke-test.
9. Telegram UX static check:
   - меню містить «🔥 Серії участі»;
   - FSM для введення днів заморозки присутній;
   - квартальний ліміт 14 днів використовується серверною create_streak_freeze;
   - кнопки каталогу активностей містять XP;
   - multiple-choice опитування оновлює reply_markup того самого повідомлення й має «✅ Готово».
10. Web leaderboard/streak:
   - 6 ліг;
   - TOP-10 використовує окремі rank/name/XP колонки;
   - окрема сторінка /admin/streaks/{user_id};
   - ручна корекція, freeze, history, rebuild присутні.
11. Broadcast UI: аудиторія «Усі учасники зі статусом «Активний»» підписана явно; backend і раніше фільтрує active.
12. Goals/badges measurement: додані tasks, surveys, activities, ideas, badges/referrals/streak metrics у відповідних модулях.
13. Web confirmation of pending registration: окремий activate flow; superadmin активує одразу, admin створює pending status-change request.
14. Export smoke-tests:
   - базовий event XLSX — PASS;
   - sensitive event XLSX — PASS;
   - базовий branded event PDF — PASS, PDF render visually checked;
   - sensitive event PDF — PASS;
   - автоматичний report XLSX — PASS/openpyxl reopen;
   - автоматичний branded report PDF — PASS, first page visually checked.
15. Event lifecycle: open/closed/postponed events автоматично переходять у completed після завершення серверного event window; postponed не скидається звичайним редагуванням.

Обмеження sandbox-перевірки:
- У середовищі немає встановленого aiosqlite/aiogram runtime, тому реальний Telegram polling і async SQLite Database.init не запускались.
- Натомість Python syntax/AST, templates, CSS, sync SQLAlchemy schema compatibility та експортні генератори перевірені окремо.
- Production BOT_TOKEN/Heroku/PostgreSQL не змінювались під час підготовки пакета.

Перед production deploy: обов’язковий Heroku PG backup, /health check і короткий smoke-test Telegram після запуску.
16. Fresh-extract verification фінального ZIP: archive clean before compile; compile/AST/Jinja/CSS/routes/version/league boundaries repeated successfully.
