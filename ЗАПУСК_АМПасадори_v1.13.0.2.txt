АМПасадори v1.13.0.2 — Startup Import Hotfix

1. Перейти у локальний Git-репозиторій:
   cd ~/Downloads/amp_bot_v1103

2. Рекомендований backup production PostgreSQL:
   heroku pg:backups:capture -a amp-bot-ver-1-5-0

3. Розпакувати build:
   rm -rf /tmp/amp_bot_v11302
   mkdir -p /tmp/amp_bot_v11302
   unzip -q ~/Downloads/amp_bot_v11302.zip -d /tmp/amp_bot_v11302

4. Накласти build поверх репозиторію:
   rsync -av \
     --exclude='.git' \
     --exclude='.env' \
     --exclude='.venv' \
     --exclude='__pycache__' \
     --exclude='.pytest_cache' \
     /tmp/amp_bot_v11302/amp_bot_v11302/ \
     ./

5. Перевірити версію:
   cat VERSION.txt
   # очікується 1.13.0.2

6. Локальні перевірки:
   python -m compileall -q app scripts tests migrations
   python -m scripts.production_preflight
   pytest -q tests/test_v11302_startup_import_hotfix.py tests/test_v11301_ci_compat_hotfix.py tests/test_v1130_architecture_completion.py

7. Commit + GitHub Production Gate:
   git status --short
   git add -A
   git commit -m "Hotfix AMP v1.13.0.2 startup imports"
   git push origin main

8. Дочекатися повністю зеленого GitHub Actions. Особливо перевірити:
   - full pytest/regression;
   - Run python -m scripts.heroku_release;
   - startup smoke import app.main;
   - backup/deploy jobs.

9. Після deploy:
   heroku ps -a amp-bot-ver-1-5-0
   # очікується web.1 up, worker.1 up

10. Alembic head:
    heroku run -a amp-bot-ver-1-5-0 -- alembic current
    # очікується 20260915_0002 (head)

11. Health:
    curl -sS https://amp-bot-ver-1-5-0-9632a1434a6d.herokuapp.com/health/ready | python3 -m json.tool
    curl -sS -o /tmp/amp_dependencies.json -w 'HTTP %{http_code}\n' https://amp-bot-ver-1-5-0-9632a1434a6d.herokuapp.com/health/dependencies
    cat /tmp/amp_dependencies.json | python3 -m json.tool

12. Worker log:
    heroku logs --dyno worker -n 180 -a amp-bot-ver-1-5-0

13. Ручний smoke:
    - Telegram: /start, /menu, Події, Квести, Волонтерство, Можливості.
    - Web: login + 2FA, Dashboard.
    - Event Cockpit/scanner/check-in.
    - Analytics та Reports.
    - worker + 13 scheduler healthy.

ВАЖЛИВО:
- production PostgreSQL НЕ очищати;
- нової migration у v1.13.0.2 немає;
- Alembic head залишається 20260915_0002;
- не запускати deploy, якщо GitHub gate червоний.
