AMP XP / АМПасадори v1.7.3 — FINAL TEST REPORT
Дата: 2026-09-12

1. VERSION
- VERSION.txt = 1.7.3
- app/version.py APP_VERSION = 1.7.3

2. STATIC CODE
- Python compileall: OK
- AST parse: 41 app+scripts Python files: OK
- Jinja parse: 40 templates: OK
- CSS parse: 0 syntax errors
- FastAPI static route scan: 147 routes, duplicate method+path = 0

3. CSRF
- CSRFMiddleware protects POST/PUT/PATCH/DELETE under /admin.
- Static template scan: 0 POST forms without nearby _csrf hidden token.
- base.html additionally injects the current session token into POST forms.
- Session cookie: SameSite=Strict; secure flag follows COOKIE_SECURE/Heroku.

4. WEB AUTH
- New DB models: web_staff_accounts, web_admin_sessions.
- Password hashing: PBKDF2-SHA256, random salt, 310000 iterations.
- Password policy smoke-test: >=12 chars, upper/lower/digit/special; username excluded.
- 5 failed attempts -> 15-minute lock.
- Temporary password -> must_change_password before access to other admin pages.
- Superadmin 2FA is mandatory; admin 2FA optional.
- One-time code: 6 digits, 10-minute TTL, max 5 attempts, delivered via Telegram.
- Server-side sessions: token stored in cookie, only SHA-256 token hash stored in DB; revoke supported.

5. MEDIA PRIVACY
- Explicit categories: public / participant_private / staff_private / superadmin_private.
- Unknown category defaults to staff_private.
- /media, legacy /uploads and local /private no longer expose private categories anonymously.
- Sensitive media: private,no-store + nosniff; access is audited.
- Security primitive smoke test for category mapping: OK.

6. AUDIT
Static verification found audit events for:
- login/logout/failed login/lock/2FA
- password change/reset and session revoke
- sensitive media views and sensitive event exports
- role changes and XP changes
- badge operations
- streak operations
- consent operations
- destructive/admin lifecycle actions

7. PRIVACY THRESHOLD
- Analytics vulnerability counts 1-4 -> display '<5'; chart value suppressed to 0.
- Period reports use the same rule for tables and charts.
- No row-level vulnerability export was added.

8. DATABASE COMPATIBILITY
- v1.7.2 sync schema: 40 tables.
- v1.7.3 sync schema after additive create_all: 42 tables.
- New tables: web_staff_accounts, web_admin_sessions.
- Upgrade simulation v1.7.2 -> v1.7.3: OK.
- Re-running create_all on upgraded schema: OK (idempotent table creation).

9. DEPLOYMENT SAFETY
- Legacy WEB_ADMIN_PASSWORD / WEB_STAFF_ACCOUNTS_JSON are bootstrap-only.
- Existing DB accounts are not overwritten from env on subsequent boots.
- After successful first v1.7.3 login and account verification, plaintext legacy vars must be removed from Heroku Config Vars.
- No migrate_sqlite_to_postgres.py is required.

10. PACKAGE HYGIENE
- Final package must not contain .env, DB files, data, __pycache__, *.pyc.

LIMITATION OF THIS TEST ENVIRONMENT
- aiogram and aiosqlite are not installed in the sandbox and internet package installation is unavailable, so a live Telegram polling run and full async SQLite/FastAPI runtime smoke test were not executed here.
- Python syntax/AST, templates, CSS, security primitives, route uniqueness and sync SQLAlchemy schema upgrade were verified.
