АМПасадори v1.13.0.3 — Telegram Navigation & Web Localization Hotfix

1. Перейти у локальний Git-репозиторій:
   cd ~/Downloads/amp_bot_v1103

2. Рекомендований backup production PostgreSQL:
   heroku pg:backups:capture -a amp-bot-ver-1-5-0

3. Розпакувати build:
   rm -rf /tmp/amp_bot_v11303
   mkdir -p /tmp/amp_bot_v11303
   unzip -q ~/Downloads/amp_bot_v11303.zip -d /tmp/amp_bot_v11303

4. Накласти build поверх репозиторію:
   rsync -av \
     --exclude='.git' \
     --exclude='.env' \
     --exclude='.venv' \
     --exclude='__pycache__' \
     --exclude='.pytest_cache' \
     /tmp/amp_bot_v11303/amp_bot_v11303/ \
     ./

5. Перевірити версію:
   cat VERSION.txt
   # очікується 1.13.0.3

6. Локальні перевірки:
   python -m compileall -q app scripts tests migrations
   python -m scripts.production_preflight
   pytest -q \
     tests/test_v11303_telegram_web_ux_hotfix.py \
     tests/test_v11302_startup_import_hotfix.py \
     tests/test_v11301_ci_compat_hotfix.py \
     tests/test_v1130_architecture_completion.py \
     tests/test_v181_features.py \
     tests/test_v192_smart_opportunities_seasons.py \
     tests/test_v1110_ux_conversion.py \
     tests/test_v1821_calendar_event_ops.py

7. Commit + GitHub Production Gate:
   git status --short
   git add -A
   git commit -m "Hotfix AMP v1.13.0.3 Telegram navigation and web localization"
   git push origin main

8. Дочекатися повністю зеленого GitHub Actions. Не deploy-ити вручну при червоному gate.

9. Після deploy перевірити процеси:
   heroku ps -a amp-bot-ver-1-5-0
   # очікується web.1 up, worker.1 up

10. Alembic head:
    heroku run -a amp-bot-ver-1-5-0 -- alembic current
    # очікується 20260915_0002 (head)

11. Health:
    curl -sS https://amp-bot-ver-1-5-0-9632a1434a6d.herokuapp.com/health/ready | python3 -m json.tool
    curl -sS -o /tmp/amp_dependencies.json -w 'HTTP %{http_code}\n' https://amp-bot-ver-1-5-0-9632a1434a6d.herokuapp.com/health/dependencies
    cat /tmp/amp_dependencies.json | python3 -m json.tool

12. Worker log:
    heroku logs --dyno worker -n 180 -a amp-bot-ver-1-5-0

13. Обов'язковий Telegram smoke:
    - /start — активному користувачу має прийти «Головна» + актуальна reply-клавіатура;
    - /menu — має відповісти й повторно встановити актуальну reply-клавіатуру;
    - /smart — має відкрити персональні можливості;
    - Винагороди — назви мають бути повними, без програмного обрізання;
    - перевірити нове розміщення кнопок «Головна / Долучитися / Можливості / Підтримати / Мій профіль / QR-бейдж / Ще».

14. Web smoke:
    - login + 2FA;
    - Dashboard;
    - Профіль учасника 360°;
    - Запрошення друзів / Хронологія;
    - Сезони та історія;
    - Календар / Заморозка серії;
    - Notification Center: видимі типи та статуси українською;
    - Event Cockpit.

ВАЖЛИВО:
- production PostgreSQL НЕ очищати;
- нової migration у v1.13.0.3 немає;
- Alembic head залишається 20260915_0002;
- `/menu` оновлює reply-клавіатуру вже після успішного deploy нового worker;
- `/smart` з'явиться у slash-командах після успішного `configure_bot_profile` на worker startup.
